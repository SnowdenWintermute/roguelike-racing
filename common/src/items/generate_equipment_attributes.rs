use super::affixes::{self, Affix, PrefixTypes, SuffixTypes};
use crate::primatives::Range;
use once_cell::sync::Lazy;
use std::collections::HashMap;

pub fn generate_equipment_attributes() {
    //
}
