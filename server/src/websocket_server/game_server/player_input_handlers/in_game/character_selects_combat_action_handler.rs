use crate::websocket_server::game_server::GameServer;
use common::errors::AppError;

impl GameServer {
    pub fn character_selects_combat_action_handler() -> Result<(), AppError> {
        Ok(())
    }
}
